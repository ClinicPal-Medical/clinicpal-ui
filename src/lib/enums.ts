export enum FormFieldTypes {
  INPUT = "input",
  TEXTAREA = "textarea",
  SELECT = "select",
  CHECKBOX = "checkbox",
  RADIO = "radio",
  PHONEINPUT = "phoneInput",
  DATEPICKER = "datePicker",
}

export enum UserRoles {
  ADMIN = "admin",
  DOCTOR = "doctor",
  STAFF = "staff",
}
